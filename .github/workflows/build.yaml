name: Build and Push

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    uses: kkato/ci/.github/workflows/docker-build-push.yml@main
    permissions:
      contents: read
      packages: write
